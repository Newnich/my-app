# my-app

Minimal Node starter app.

## Scripts

- `npm start` runs the app once.
- `npm run dev` runs the app with Node watch mode.

## Environment

- `PORT` sets the displayed application port. Default: `3000`.
